name: Actions-2
on:
  workflow_dispatch:
  watch:
    types: started
  #push:
  #  branches: [ main ]
  repository_dispatch:
    types: 同步
jobs:
  repo-sync:
    env:
      PAT: ${{ secrets.PAT || github.event.client_payload.PAT }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          persist-credentials: false

      - name: 同步
        uses: repo-sync/github-sync@v2
        if: env.PAT
        with:
          source_repo: "https://github.com/DJ-clamp/BaseSation.git"
          source_branch: "azmodan"
          destination_branch: "azmodan"
          github_token: ${{ secrets.PAT || github.event.client_payload.PAT }}
